Confirmed. Producing the phase docs now. Starting with 010a — schema only, the cleanest phase and the foundation everything else depends on.Viewed a file, created a fileViewed a file, created a fileNow running the readiness check inline before producing the build prompt.
CHECK 1 — AC: All 10 ACs testable. All marked manual test (schema changes require DB inspection). No vague language. ✓
CHECK 2 — Constraints: All name specific files. Risk MEDIUM — no feature flag required. ✓
CHECK 3 — Compliance: No compliance rules applicable to schema. ✓
CHECK 4 — Architecture: randomUUID() via $defaultFn ✓. No Orval codegen ✓. Backward compatible ✓. No propagation calls ✓. Insert-if-not-exists seed pattern ✓.
One flag: the notes rename assumption in Section 9 — Drizzle's push may not handle column renames safely. This is correctly called out as a BLOCKER if it can't. ✓
CHECK 5 — Completeness: All 11 sections present. ✓
CHECK 7 — Build simulation: One gap — the belief seed data includes cluster_display_name and cluster_tagline on cluster-specific beliefs but not on universal or company_conviction beliefs. The schema allows null for these fields. Consistent. ✓
The notes → notes_legacy rename via drizzle-kit push is the highest build risk. Adding an explicit instruction to the build prompt that this must be raised as a BLOCKER if drizzle-kit push cannot handle it safely.
⚠️ WARNING: Touches leadsTable schema (production data) and documentsTable. drizzle-kit push required. Confirm safe before executing.
VERDICT: READY FOR BUILDCreated a file, read a fileCreated a file, read a file⚠️ WARNING: Touches leadsTable (production data) and documentsTable. drizzle-kit push required — confirm safe before executing.
Paste RR_UNLOCK_500_010a_build_prompt_v1.md into Replit AI. It will immediately raise the BLOCKER about the notes column rename. When it does, inspect lib/db/src/schema/leads.ts to see the exact current column definition, then decide:

If notes is a plain jsonb column with default([]) and all existing leads have empty arrays — safe to proceed with Option A (rename + new field)
If any leads have actual data in notes — still safe with Option A since it preserves the data in notes_legacy

Either way Option A is safe. Tell Replit AI to proceed with Option A and return the build output here.