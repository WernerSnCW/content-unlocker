CRITICAL INSTRUCTION — READ THIS BEFORE DOING ANYTHING ELSE:

Your only job is to fill in the template below exactly as written.
Do not summarise. Do not write prose. Do not say "what would you like
to work on next." Output the completed template and nothing else.
Every bracketed placeholder must be replaced with real content from
code inspection. Stop when the template is complete.

---

# 100 — CURRENT STATE PROMPT
Version: 1.4

Give me a complete overview of this codebase. Complete the template below
exactly. Do not add prose before or after it. Replace every placeholder
in square brackets.

SCOPE: Scope this scan to the following files only:
- artifacts/api-server/src/routes/generation/index.ts
- artifacts/api-server/src/lib/generationEngine.ts
- artifacts/api-server/src/routes/documents/index.ts
- lib/db/src/schema/documents.ts

---

SIGNIFICANCE RULE:
A file is significant if it defines business logic, routing, persistence,
validation, shared types, UI composition, tests, configuration, migrations,
or integration code. When uncertain: include it.

ANTI-INFERENCE RULE:
Describe only what actually exists in the code. If a file's purpose is
unclear from code inspection, write: "Purpose unclear from scan." Do not
infer intent from filenames.

---

## CURRENT STATE OUTPUT

### 0. Scan coverage
**Scope:** scoped to: artifacts/api-server/src/routes/generation/index.ts,
artifacts/api-server/src/lib/generationEngine.ts,
artifacts/api-server/src/routes/documents/index.ts,
lib/db/src/schema/documents.ts
**Fully scanned:** [list files actually read]
**Summarised at directory level:** [list, or "none"]
**Could not reach:** [list, or "none"]
**Limitations:** [any known gaps, or "none"]

---

### 1. Key files

#### `artifacts/api-server/src/routes/generation/index.ts`
**What it does:** [one sentence from code inspection]
**Key functions, variables, data structures:** [list every route handler,
its method + path, request body fields, response shape, and which lib
functions it calls — be specific]

#### `artifacts/api-server/src/lib/generationEngine.ts`
**What it does:** [one sentence from code inspection]
**Key functions, variables, data structures:** [list every exported
function, its parameters, return type, and what it does — be specific.
Include the exact request shape accepted by generateFromTemplate() and
the exact response shape it returns]

#### `artifacts/api-server/src/routes/documents/index.ts`
**What it does:** [one sentence from code inspection]
**Key functions, variables, data structures:** [list every route handler,
its method + path, and what fields it reads/writes to the documents table]

#### `lib/db/src/schema/documents.ts`
**What it does:** [one sentence from code inspection]
**Key functions, variables, data structures:** [list every column in the
documents table with its type and any default values]

---

### 2. Data model

- **[Type or schema name]:** [its fields and types]

---

### 3. Data flow

How a document gets created and persisted:

- **[Step]:** [what happens, which function, what it writes]

---

### 4. Dependencies between files

- **[File or function]:** depends on [what] for [reason]

---

### 5. Test coverage

If no tests exist: "No tests currently exist."

---

### 6. External services

- **[Service]:** [how it is used in these files]

---

### 7. Patterns and conventions

- [pattern relevant to generation or document persistence]

---

### 8. Inconsistencies and flags

- [flag, or "None identified"]